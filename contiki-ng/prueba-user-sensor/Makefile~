CONTIKI_PROJECT = test_sensor

TARGET ?= zoul
BOARD ?= remote-revb
PORT ?= /dev/ttyUSB0

all: $(CONTIKI_PROJECT)

CONTIKI = ../../
CFLAGS += -DPROJECT_CONF_H=\"project-conf.h\"

include $(CONTIKI)/Makefile.include

